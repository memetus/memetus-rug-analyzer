interface IBaseChecker {}

/**
 * Base class for all checkers.
 * @implements {IBaseChecker}
 * @description Every checker class should extend this class.
 */
export class BaseChecker implements IBaseChecker {}
